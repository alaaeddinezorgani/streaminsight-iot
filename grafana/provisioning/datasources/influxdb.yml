apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    isDefault: true
    jsonData:
      version: "Flux"
      defaultBucket: "iot_bucket"
      organization: "streaminsight"
    secureJsonData:
      token: "admin:adminpass"

