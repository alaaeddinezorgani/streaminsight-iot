sudo docker compose exec influxdb bash

influx bucket list -o streaminsight


influx auth create \
    -o streaminsight \
    -t admin-token \
    -d grafana-token \
    --read-bucket <BUCKET_ID>


example:
influx auth create     -o streaminsight     -t admin-token     -d grafana-token     --read-bucket ceeba7f063cc324c

